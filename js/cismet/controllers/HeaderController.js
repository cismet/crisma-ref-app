angular.module(
    'de.cismet.smartAdmin.controllers'
    ).controller('HeaderCtrl',
    [
        '$scope',
        'LayoutService',
        function ($scope, LayoutService) {
           
        }
    ]);
